{
  "keyboard_shortcut": "", 
  "name": "PUBLISH-JavaScript Escape HTML", 
  "language": "JavaScript", 
  "trigger": "trigger_postopen", 
  "value": [
    "if (komodo.view) { komodo.view.setFocus(); }", 
    "", 
    "/*", 
    " * Replace '<' with '&lt;' in script for upload to html", 
    " * This way is to disable html tags", 
    " *", 
    " * Reuslt is copied to clipboard and function is UNDUED", 
    " *", 
    "*/", 
    "\t", 
    "\t\t", 
    "\t/* NASTAVENO PRO LETNI CAS  */  ", 
    "\t//var newDate = new Date();", 
    "\t//newDate.setTime(newDate.getTime() + (2*60*60*1000)); // add 2 hours", 
    "\t//dateString = newDate.toUTCString();", 
    "\t", 
    "\t//komodo.assertMacroVersion(3);", 
    "\t//if (komodo.view) { komodo.view.setFocus(); }", 
    "\t//", 
    "\t//ko.views.manager.currentView.scimoz.beginUndoAction();", 
    "\t//try {", 
    "\t//\t/* REPLACE PLACEHOLDERS */", 
    "\t\tko.find.replaceAllInMacro(window, 0, '<', '&lt;', false, 2, 0, false, false);", 
    "\t//\t", 
    "\t//\t/* SET FILE HEADER */", 
    "\t//\tsetTimeout( function(){ ", 
    "\t//\t\tko.commands.doCommand('cmd_selectAll');", 
    "\t//\t\tko.commands.doCommand('cmd_copy');", 
    "\t//\t\t", 
    "\t//\t}, 100); ", 
    "\t//", 
    "\t//} finally {", 
    "\t//\tko.views.manager.currentView.scimoz.endUndoAction();", 
    "\t//}", 
    "\t//", 
    "\t//setTimeout( function(){ ", 
    "\t//\tko.commands.doCommand('cmd_undo');", 
    "\t//}, 200); ", 
    "\t//"
  ], 
  "version": "1.0.12", 
  "rank": 100, 
  "trigger_enabled": false, 
  "async": false, 
  "type": "macro"
}